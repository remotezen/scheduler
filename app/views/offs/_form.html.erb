<%= form_with(model: off, local: true) do |form| %>
  <% if off.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(off.errors.count, "error") %> prohibited this off from being saved:</h2>

      <ul>
      <% off.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.datetime_select :start, id: :off_start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.datetime_select :end, id: :off_end %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :off_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
